server:
  port: 8901
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kuonglv/springcloud-learning/springcloud-config.git
          # username: kuonglv
          # password: 123456
          clone-on-start: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/